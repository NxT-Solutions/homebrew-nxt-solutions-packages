# nxt-solutions-packages

Homebrew tap for [NxT Solutions](https://github.com/NxT-Solutions).

```bash
brew tap NxT-Solutions/nxt-solutions-packages
```

- CLI tools live in `Formula/`.
- macOS apps live in `Casks/`.

Old `NoahNxT/homebrew-nxt-solutions-packages` URLs redirect here.

